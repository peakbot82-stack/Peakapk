name: Build APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: Install build dependencies
      run: |
        sudo apt update
        sudo apt install -y \
          git zip unzip openjdk-17-jdk \
          python3-pip autoconf libtool \
          pkg-config zlib1g-dev \
          libncurses5-dev libncursesw5-dev \
          libtinfo5 cmake libffi-dev \
          libssl-dev liblzma-dev \
          libjpeg-dev libxml2-dev libxslt1-dev
    
    - name: Install Python packages
      run: |
        pip install --upgrade pip
        pip install buildozer cython setuptools wheel
        pip install -r requirements.txt
    
    - name: Build APK with Buildozer
      run: |
        buildozer android debug
    
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: predictor-pro-apk
        path: bin/*.apk
        compression-level: 0 # Opcional: mejora la velocidad de subida
        if-no-files-found: error # Falla si no encuentra el APK
